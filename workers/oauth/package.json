{
  "name": "oauth-do-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build:web": "cd web && pnpm install && pnpm build",
    "deploy": "pnpm build:web && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@dotdo/oauth": "workspace:*",
    "@workos-inc/node": "^7.0.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
